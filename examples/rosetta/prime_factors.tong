// Rosetta Code: Prime decomposition
// Use integer division to keep values integral
fn idiv(n, d) {
    var q = 0
    var m = n
    while m >= d {
        m = m - d
        q = q + 1
    }
    return q
}

fn prime_factors(n) {
    var n = n
    var d = 2
    while d * d <= n {
        while n % d == 0 {
            print(d)
            n = idiv(n, d)
        }
        d = d + 1
    }
    if n > 1 { print(n) }
}

prime_factors(13195)
