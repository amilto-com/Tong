// Rosetta Code: Towers of Hanoi
fn hanoi(n, from, to, aux) {
    if n == 0 {
        // do nothing
    } else {
        hanoi(n-1, from, aux, to)
        print("Move", from, "->", to)
        hanoi(n-1, aux, to, from)
    }
}

hanoi(4, "A", "C", "B")
